// Re-export RBAC-relevant types from shared/types for convenience within the rbac module
export type {
  RoleName,
  FeatureCategory,
  PermissionLevel,
  PermissionMatrixData,
  RolePermissions,
  TabId,
} from '../types';
